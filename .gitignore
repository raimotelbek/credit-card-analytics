# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Data — raw CSVs and the local DuckDB warehouse are large + reproducible
data/raw/
data/warehouse.duckdb
data/*.duckdb

# Per-query CSV outputs are reproducible from sql/ + a loaded warehouse
data/processed/query_outputs/

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp

# Credentials — never commit
.kaggle/
gcp-key*.json
.env
.env.*

# Jupyter
.ipynb_checkpoints/
