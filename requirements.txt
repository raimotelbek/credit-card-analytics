pandas>=2.0
numpy>=1.24
duckdb>=0.9
matplotlib>=3.7

# BigQuery (optional — only needed for production load to GCP)
google-cloud-bigquery>=3.10
pandas-gbq>=0.19
pyarrow>=12.0

# Kaggle (optional — only needed if pulling the real dataset)
kaggle>=1.5
